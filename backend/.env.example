# ==========================================
# 应用环境配置
# ==========================================

# 运行环境：development/dev（开发环境）、production/prod（生产环境）
# 默认：development
APP_ENV=development

# ==========================================
# Webhook 配置
# ==========================================

# Infisical Webhook 签名验证密钥（必需，如果要使用 Webhook 功能）
# 从 Infisical 控制台获取此密钥
INFISICAL_WEBHOOK_SECRET=your_secret_key_here

# ==========================================
# 数据库配置
# ==========================================

# SQLite 数据库文件路径
# 默认：backend/data/todos.db（项目根目录运行）或 data/todos.db（backend 目录运行）
TODO_DB_PATH=./data/todos.db

# ==========================================
# HTTP 服务配置
# ==========================================

# HTTP 服务监听端口
# 默认：8080
TODO_BIND_ADDR=8080

# 请求体最大大小（字节）
# 默认：10485760（10MB）
# 示例：5242880（5MB）、20971520（20MB）
TODO_MAX_BODY_SIZE=10485760

# ==========================================
# CORS 跨域配置
# ==========================================

# 允许的跨域来源列表，多个域名用逗号分隔
# 开发环境：为空时自动允许 localhost 和 127.0.0.1 的所有端口
# 生产环境：应设置具体的域名
# 示例：https://example.com,https://app.example.com
CORS_ALLOWED_ORIGINS=
