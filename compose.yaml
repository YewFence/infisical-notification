services:
  frontend:
    image: ghcr.io/yewfence/infisical-notification-frontend:latest
    container_name: yewtally-frontend
    env_file: .env
    ports:
      - "${NGINX_PORT:-5473}:${NGINX_PORT:-5473}"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - yewtally-net

  backend:
    image: ghcr.io/yewfence/infisical-notification-backend:latest
    container_name: yewtally-backend
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:${TODO_BIND_ADDR:-8080}/health"]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - yewtally-net

networks:
  yewtally-net:
    driver: bridge
